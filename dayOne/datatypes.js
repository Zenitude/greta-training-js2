// 5. Créez un fichier datatypes.js et utilisez l'opérateur typeof pour vérifier différents types de données. Vérifiez le type de données de chaque variable

let uneChaine = 'une chaine de caractère';
let unBolean = true;
let variableNonDefini;
let uneVariableNull = null;

console.log(`La variable chaine est de type ${typeof chaine}`);
console.log(`La variable bolean est de type ${typeof bolean}`);
console.log(`La undefined chaine est de type ${typeof undefined}`);
console.log(`La variable variableNull est de type ${typeof variableNull}`);

// 6. Déclarez quatre variables sans affecter de valeurs

let variable1, variable2, variable3, variable4;

// 7. Déclarez quatre variables avec des valeurs affectées

let date = '01/01/2022';
let jour = 1;
let mois = 1;
const annee = 2021;

// 7. Déclarez des variables pour stocker votre prénom, nom, état matrimonial, pays et âge sur plusieurs lignes

let prenom = 'Rudy';
let nom = 'Mollet';
let etatMatrimonial = 'Célibataire';
let pays = 'France';
let age = 33;

// 9. Déclarez des variables pour stocker votre prénom, nom, état matrimonial, pays et âge sur une seule ligne

let monPrenom = 'Rudy', monNom = 'Mollet', monEtatMatrimonial = 'Célibataire', monPays = 'France', monAge = 33;

// 10. Déclarez deux variables myAge et yourAge et initialisez ces deux variables et utilisez la console du navigateur.

let myAge = 33;
let yourAge = 26;

console.log(`J'ai ${myAge} ans.`);
console.log(`Vous avez ${yourAge} ans.`);

